basePath: /
host: localhost:8080
info:
  contact: {}
  description: Bookmark API
  title: Bookmark API
paths:
  /gen-pass:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: Generate password
      tags:
      - Password
swagger: "2.0"
